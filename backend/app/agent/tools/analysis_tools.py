"""ADK helper tools for analysis over fetched rows (pure computation, no I/O).

Examples: compute estimation skew, rank review bottlenecks, detect back-to-back meeting blocks.
Keep these deterministic and unit-testable.
"""

# TOOLS = [...]
TOOLS: list = []  # TODO: populate with analysis helper functions
