"""Connector domain model (GitHub / Jira / Calendar / PagerDuty sync status)."""

# class Connector(BaseModel):
#     id: str                # github | jira | calendar | pagerduty
#     status: str            # connected | syncing | error
#     last_sync_at: str | None
