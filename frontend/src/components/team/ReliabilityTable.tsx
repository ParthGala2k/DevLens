"use client";
// Developer observability: who consistently completes assigned work vs who lags.
// TODO: GET /api/team/reliability; render a sortable table (completion ratio, cycle time, on-time %).
import { Card } from "@/components/ui/Card";

export function ReliabilityTable() {
  return <Card title="Completion Reliability">{/* TODO: reliability table */}</Card>;
}
